__pycache__/
*.pyc
*.pyo
.preview/

# Node build tooling and generated output (run `npm install && npm run build`)
node_modules/
dist/

# Secrets — never commit environment files
.env
.env.*
!.env.example

# Transient render of health-report.json, regenerated every run purely as the
# body for the data-health tracking issue. The JSON is the artifact worth keeping.
health-report.md
